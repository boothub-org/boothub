port = (System.getenv('PORT') ?: 8080) as int
browserAutoStart = false
// outputDirBasePath = "/tmp/boothub"
// zipFilesBasePath = "/tmp/boothub/zip"

repoManager = new org.boothub.repo.DBRepoManager(
                    new org.boothub.repo.heroku.HerokuDBApi(),
                    new org.boothub.repo.postgresql.PGJobDAO(),
                    new org.boothub.repo.DefaultRepoCache()
                )
